index page - gameroom?

<% @puzzles.each do |p| %>
    <p>puzzle #<%= p.id %>) <strong><%= p.title %></strong> 
        <% if current_player == p.created_by %>
            <a href="/puzzles/<%= p.id %>/edit">Edit</a> 
        <% end %>
    </p>
<% end %>